{
  "name": "charon-lambda",
  "version": "1.0.0",
  "description": "Lambda to interact with an obscure VPN service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aws-lambda",
    "streisand",
    "vpn",
    "aws"
  ],
  "author": "RCH",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.441.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "serverless-http": "^2.0.0"
  },
  "devDependencies": {
    "serverless-offline": "^4.9.4"
  }
}
